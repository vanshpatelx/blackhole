# getblackhole.app

The landing page. Plain HTML and CSS, no build step, served from Cloudflare.

```bash
open index.html          # edit and refresh
npx wrangler deploy      # publish to getblackhole.app
```

Screenshots are copied from `docs/images/`; refresh them with `make demo` in the repo root and a new
capture when the UI changes.
