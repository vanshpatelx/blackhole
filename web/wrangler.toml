name = "blackhole"
compatibility_date = "2026-09-01"

# The landing page is plain static files served straight from this folder.
[assets]
directory = "."

routes = [
  { pattern = "getblackhole.app", custom_domain = true },
  { pattern = "www.getblackhole.app", custom_domain = true }
]
